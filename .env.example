# OpenCode MCP Configuration
# Copy this to .env and fill in the values

# Context7 (optional — free tier works without API key)
# Sign up at https://context7.com to get higher rate limits
CONTEXT7_API_KEY=
